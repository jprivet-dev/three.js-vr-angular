import { Lensflare } from 'three/examples/jsm/objects/Lensflare';
import { Decorator } from './decorator';

export class LensfareDecorator extends Decorator {
  constructor(lensflare: Lensflare) {
    super(lensflare);
  }
}
