import { createAction } from '@ngrx/store';

export const switchStats = createAction('[App] switch stats');

export const switchDefinition = createAction('[App] switch definition');

export const switchAntialias = createAction('[App] switch antialias');
